name: xapres
channels:
  - conda-forge
dependencies:
  # Required for full project functionality (dont remove)
  - pytest
  # Examples (remove and add as needed)
  - jupyter
  - jupyterlab
  - jupyter-book
  - xarray
  - numpy
  - scipy
  - dask
  - netcdf4
  - pandas
  - matplotlib
  - hvplot
  - intake
  - fsspec
  - gcsfs
  - tqdm
  - pip
  - zarr
  - pip:
      - ipympl
      - sounddevice
      - soundfile
      - sphinx-inline-tabs
      - sphinx-examples
      - sphinx-proof
      - sphinx-hoverxref
      - sphinx-autodoc2
      - jupyter-book
