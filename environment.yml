name: xapres
channels:
  - conda-forge
dependencies:
  - pytest
  - gcsfs
  - numpy
  - xarray
  - pandas
  - tqdm
  - pip
  - zarr
  - pip:
      - ipympl
      - sounddevice
      - soundfile
      - bas-apres
